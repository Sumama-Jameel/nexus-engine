# V11 built-in mode: dev
# Loaded from //go:embed in internal/mode/mode.go
# Override per-machine by placing a file with the same name in ~/.nexus/modes/.
name: dev
description: Developer workstation — full toolchain, services enabled
profile: base-dev
dotfiles_source: ""
stop_services: []
start_services: [sshd]
os_tweaks:
  linux:
    cpu_governor: balanced
  windows:
    power_plan: balanced
